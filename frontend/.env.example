# Point this at your Express server once the backend exists.
# services/api.js is the ONLY place this value is read.
VITE_API_URL=http://localhost:5000/api

# Flip to "false" to stop using mock data and hit the real backend instead.
VITE_USE_MOCKS=true
